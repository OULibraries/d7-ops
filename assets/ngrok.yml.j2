authtoken: {{ ngrok_authtoken }}
log_format: json
log_level: debug
log: /vagrant/ngrok.log
tunnels:
  {{ site }}-tls:
    hostname: {{ site }}.{{ httpd_dn_suffix }}
    proto: tls
    addr: 443
  {{ site }}-http:
    hostname: {{ site }}.{{ httpd_dn_suffix }}
    proto: http
    addr: 80
